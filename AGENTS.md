# 项目规则

## 0. 总体原则

所有生成的内容（页面、PRD、数据）必须与 `02-需求文档`中的文件保持一致，并 html 风格严格遵循`.Codex` 中的 UI_design_APP 或 UI_design_WEB 文件（根据页面类型确定）。

## 1. 需求与设计一致性
- 绘制的每个页面都必须与 PRD 文档中的要求完全对应，不得遗漏功能点。
- 所有页面（包括弹窗、二级页面、状态提示等）必须遵循工作区 UI 设计规范，包含颜色、字体、图标、间距、圆角等。
- 即便 PRD 文档未明确要求，但按照业务逻辑明显存在的页面（如：表单提交后的成功弹窗、删除确认框、详情页、编辑页等），也必须绘制。

## 2. PRD 撰写与变更
- PRD 撰写需要调用 prd-writer skill 进行撰写
- 用户提出新需求或变更时，同时更新：
  - 对应的 HTML 页面（按新需求重新绘制）
  - PRD 文档（位于 `02-产品文档`）
- **版本记录规则**：同一天的更新合并为一条版本记录
- 除非我明确要求全文重构，否则任何场景禁止重写完整文件/完整 PRD

## 3. 高保真页面绘制
- 在绘制高保真页面前，需要先读取 `.Codex` 中的 UI_design_APP 或 UI_design_WEB 文件，明确 UI 风格和设计要求
- 在进行页面绘制时，优先调用/frontend-design skill 进行页面的绘制
- **公共组件优先引用**：绘制页面时，建议优先引用 `03-高保真页面/components/` 下已有匹配的公共组件；若用户单独要求或组件不适用，可自行绘制
- **重复即建议抽取**：同一功能在第 3 个页面再次出现时，主动向用户提出抽取建议（含组件 API 设计），用户确认后抽取至 components/，并在 README 组件表中登记

## 4. 数据与示例
- 填充示例数据时，优先参照 `01-需求文档\示例数据参考.md` 中的内容（如果文件存在且字段匹配）。
- 若无该文件或无对应字段，可自行生成合理示例数据（符合字段类型，如姓名、时间、金额等）。

## 5. 自测与质量保证
- **重大版本新建**或**问题修复后**，必须打开浏览器进行实际验证（点击、跳转、弹窗、数据展示等）。
- 浏览器截图统一放置在 `07-bugs` 文件夹中，命名建议：`功能名_测试时间.png`；由 Stop hook 自动清理（`.claude/hooks/cleanup-bugs.ps1`），仅保留最近5张，需长期保留的图片移入 `05-参考文件`。
- **举一反三**：当发现一个问题（如某个列表没有分页、某个弹窗样式错误），需主动检查所有其他页面是否存在同类问题，并一并修复。
- 每次绘制完成后，需对照需求文档和 UI 设计规范进行一次完整检查，确保无遗漏、无偏差。

## 6. 版本管理与备份
- 版本管理以 git 提交为主，回滚用 `git checkout`；`06-备份` 仅用于交付前快照（手动触发，保留最近3次）
- 提交触发：以下时机由 AI **主动提出提交建议**（列出改动内容清单），用户确认后执行 `git commit`，提交信息由 AI 按本次实际修改内容总结摘要：
  1. 用户明确要求提交/备份
  2. 页面重绘、PRD 修改、UI 规范或 skill 修改、公共组件修改等重大变更（变更前先提交快照）
  3. 新版本页面生成完成后
- 禁止每轮对话自动提交；未经用户确认不得擅自 commit
- 如需恢复：`git checkout <commit> -- <文件>` 或查看 `git log` 定位版本

## 7. 项目结构与 Github Pages 适配

项目目录结构必须遵循以下规范（用于上传至 Github 并启用 Pages 生成公开链接）：

项目根目录/

├── index.html # 入口文件，必须放在根目录

├── README.md # 项目说明文档，需定期更新

├── 01- 需求文档/ # 用户提供的需求文档（仅当用户提及时读取）

├── 02- 产品文档/ # 系统自动生成的 PRD 文档

├── 03- 高保真页面/ # 所有绘制的 HTML 页面及 CSS、JS 等组件

├── 04- 后端代码/ # 除却前端页面代码外，后端的代码、脚本放置的文件夹

├── 05- 参考文件/ # 用户提供的参考文件（仅当用户提及时读取）

├── 06- 备份/ # 最近一次的备份内容

└── 07-bugs/ # 自测截图

> 根目录下的 `index.html` 直接跳转到 `03-高保真页面` 中的 layout 页面即可（建议使用 iframe 或导航菜单）。
> 每次生成新版本页面或者有重大变更后，应同步更新 README.md。

## 8. 对话结束输出

每次对话的最后一句话必须以 `good job！` 结尾
