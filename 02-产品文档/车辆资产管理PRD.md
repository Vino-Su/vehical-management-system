# 车辆资产管理模块 PRD（产品需求文档）

| 属性     | 内容         |
| ------ | ---------- |
| 产品名称   | 车辆管理系统     |
| 模块名称   | 车辆资产管理     |
| 文档版本   | V1.1       |
| 创建日期   | 2026-05-29 |
| 最后更新   | 2026-05-29 |
| 文档状态   | 已确认        |

---

## 版本记录

| 版本   | 修改日期       | 修改人 | 修改内容 | 备注 |
| ---- | ---------- | --- | ---- | -- |
| V1.0 | 2026-05-29 | —   | 初始创建 |    |
| V1.1 | 2026-05-29 | —   | 1.更新车辆状态枚举为9种（在库-正常使用/闲置留用/闲置可调出/维修/保养中/待报废、待分配、已报废、已处置）；2.分配后状态变更为"在库-正常使用"；3.变更日志支持导出；4.编辑按钮改为直接进入编辑态，不需勾选；5.调拨详情后续开发 |    |

---

## 1. 业务背景与使用角色

### 1.1 业务背景

车辆资产管理模块是车辆管理系统中针对已完成资产入池车辆的核心管理模块。在车辆建档模块完成车辆信息录入后，车辆数据流转至资产管理模块，形成统一的车辆资产台账。本模块为用户提供了查看、编辑、分配、导出车辆资产数据的能力，是车辆全生命周期管理中"资产管理→调拨→运行→退出"环节的关键起点。

本模块与车辆建档模块的关系：
- 车辆建档模块负责车辆数据的**录入入口**，是数据源头
- 车辆资产管理模块负责车辆资产的**查看与管理**，是建档后车辆数据的运营管理载体

### 1.2 使用角色

| 角色       | 资产管理模块中的职责                     | 客户端   |
| -------- | ----------------------------- | ----- |
| 管理员      | 查看所有资产、编辑资产信息、分配车辆、删除车辆、导出数据 | PC+APP |
| 项目经理     | 查看本项目资产、编辑资产信息、分配车辆、删除车辆、导出数据 | PC+APP |
| 财务人员     | 查看所有资产信息、导出数据                | PC    |
| 管理层      | 查看车辆资产信息                     | PC    |
| 车队队长/司机  | 查看自身项目的车辆资产信息                | APP   |

### 1.3 核心目标与价值

| 目标维度   | 说明                                |
| ------ | --------------------------------- |
| 资产台账   | 形成统一的车辆资产台账，一览车辆资产全貌             |
| 资产维护   | 支持对资产信息进行编辑更新，保持数据时效性            |
| 车辆分配   | 支持将待分配车辆分配至指定项目，实现资产流转           |
| 全生命周期档案 | 详情页承载车辆全生命周期数据（基础信息、调拨、变更日志等）   |
| 数据导出   | 支持选中导出或全量导出，满足报表和审计需求            |

---

## 2. 功能清单总览

| 编号  | 功能点       | 功能描述                              | 优先级 |
| --- | --------- | --------------------------------- | --- |
| F01 | 资产台账列表页   | 展示车辆资产数据，支持查询、重置、编辑、分配、详情、删除、导出  | P0  |
| F02 | 车辆资产详情页   | 展示车辆全生命周期数据，以Tab标签形式呈现           | P0  |
| F03 | 基础信息Tab   | 展示/编辑车辆基础信息、归属、车型等全维度字段          | P0  |
| F04 | 调拨信息Tab   | 展示该车辆的调拨记录列表，支持查询、导出             | P0  |
| F05 | 变更日志Tab   | 展示该车辆的操作变更记录                     | P0  |
| F06 | 车辆分配弹窗    | 批量分配车辆至指定项目                      | P0  |
| F07 | 删除确认      | 二次确认后逻辑删除车辆资产信息                  | P0  |
| F08 | 导出功能      | 支持选中导出或全量导出资产数据                  | P0  |

**说明：** 详情页中维保信息、合规信息、风险信息、费用信息Tab本期不建设，仅展示Tab标签入口（置灰或标注"暂未开放"），本期仅建设基础信息、调拨信息、变更日志三个Tab。

---

## 3. 功能详细设计

### 3.1 F01 资产台账列表页

#### 3.1.1 页面定位

车辆资产管理的核心入口页面，以列表形式展示所有已入池车辆的资产数据，支持多维度查询和批量操作。

#### 3.1.2 页面结构

页面自上而下分为三个区域：
1. **查询筛选区**：提供查询条件输入与操作按钮
2. **数据列表区**：以表格形式展示车辆资产数据
3. **分页区**：展示分页控件与数据统计

#### 3.1.3 查询筛选区

查询条件与查询按钮放置在同一行显示。

| 元素     | 类型         | 配置说明                                                  |
| ------ | ---------- | ----------------------------------------------------- |
| 车架号    | 文本输入框      | 默认显示，支持模糊搜索                                           |
| 国标车架号  | 文本输入框      | 默认显示，支持模糊搜索                                           |
| 车型名称（中）| 文本输入框      | 默认显示，支持模糊搜索                                           |
| 车型版本   | 下拉单选       | 默认显示                                                   |
| 网联编号   | 文本输入框      | 收起，展开后显示，支持模糊搜索                                        |
| 车型编码   | 文本输入框      | 收起，展开后显示                                               |
| 车牌号    | 文本输入框      | 收起，展开后显示，支持模糊搜索                                        |
| 历史车型编码 | 文本输入框      | 收起，展开后显示                                               |
| 车辆型号   | 文本输入框      | 收起，展开后显示                                               |
| 车辆来源   | 下拉单选       | 收起，展开后显示，枚举值：外购、租赁、融资租赁、借用、自研                         |
| 车辆状态   | 下拉单选       | 收起，展开后显示，枚举值：在库-正常使用、在库-闲置留用、在库-闲置可调出、在库-维修、在库-保养中、在库-待报废、待分配、已报废、已处置 |
| 归属项目   | 下拉单选       | 收起，展开后显示，选项来自项目列表                                      |
| 注册时间   | 日期范围选择器    | 收起，展开后显示                                               |
| 查询按钮   | 主按钮        | 点击执行查询，列表刷新为查询结果                                      |
| 重置按钮   | 次按钮        | 点击重置所有查询条件至默认状态（所有条件为空）                               |
| 展开/收起  | 文字链接       | 默认仅显示前4个查询条件，点击展开显示所有查询条件，再次点击收起                     |
| 编辑按钮   | 主按钮        | 点击打开车辆详情页的编辑态，默认进入"基础信息"Tab编辑态；不需要先勾选车辆；仅管理员、项目经理可见 |
| 车辆分配按钮 | 次按钮        | 点击打开车辆分配弹窗；需先勾选车辆；仅管理员、项目经理可见                                 |

**查询条件显示规则：**
- 默认显示前4个查询条件：车架号、国标车架号、车型名称（中）、车型版本
- 其余查询条件收起，点击"展开"后显示全部查询条件
- 查询条件与查询按钮、重置按钮在同一行排列

#### 3.1.4 数据列表区

**列表显示字段（从左到右）：**

| 序号 | 字段名      | 字段标识              | 宽度建议   | 对齐方式 | 说明         |
| -- | -------- | ----------------- | ------ | ---- | ---------- |
| 0  | 复选框      | checkbox          | 50px   | 居中   | 支持全选/单选，用于导出和分配操作 |
| 1  | 车架号      | vin_code          | 200px  | 左对齐  | VIN码       |
| 2  | 国标车架号    | gbt_vin_code      | 160px  | 左对齐  |            |
| 3  | 车型名称（中）  | model_name        | 180px  | 左对齐  |            |
| 4  | 车型版本     | model_version     | 140px  | 左对齐  |            |
| 5  | 网联编号     | iot_code          | 120px  | 左对齐  |            |
| 6  | 车型编码     | model_code        | 140px  | 左对齐  |            |
| 7  | 车牌号      | plate_no          | 120px  | 左对齐  |            |
| 8  | 历史车型编码   | old_model_code    | 140px  | 左对齐  |            |
| 9  | 车辆型号     | vehicle_model     | 140px  | 左对齐  |            |
| 10 | 车辆来源     | asset_source      | 120px  | 左对齐  | 枚举值展示      |
| 11 | 车辆状态     | vehicle_status    | 160px  | 左对齐  | 枚举值展示，使用不同颜色标签区分 |
| 12 | 归属项目     | project_name      | 160px  | 左对齐  |            |
| 13 | 注册时间     | register_time     | 180px  | 左对齐  | 格式：YYYY-MM-DD HH:mm:ss |
| 14 | 操作       | actions           | 140px  | 居中   | 详情、删除      |

**排序规则：** 默认按注册时间由近到远倒序排列。

**车辆状态标签颜色规则：**

| 车辆状态      | 标签颜色  |
| --------- | ----- |
| 在库-正常使用   | 绿色    |
| 在库-闲置留用   | 蓝色    |
| 在库-闲置可调出  | 橙色    |
| 在库-维修     | 红色    |
| 在库-保养中    | 黄色    |
| 在库-待报废    | 灰色    |
| 待分配       | 紫色    |
| 已报废       | 灰色    |
| 已处置       | 灰色    |

**操作列按钮：**

| 操作   | 触发行为                | 可见角色                  |
| ---- | ------------------- | --------------------- |
| 详情   | 打开该车辆的资产详情页面        | 所有角色                  |
| 删除   | 弹出二次确认弹窗后逻辑删除       | 管理员（所有）、项目经理（权限范围内）   |

**行交互：** 行hover高亮。

**复选框交互：**
- 表头存在全选复选框，勾选后选中当前页所有行
- 支持单行勾选
- 勾选状态用于导出和车辆分配操作

#### 3.1.5 分页区

| 配置项     | 规则                                |
| ------- | --------------------------------- |
| 每页条数选项  | 10、20、50、100，默认10                |
| 显示信息    | 总记录数、当前页码/总页数                    |
| 翻页控件    | 首页、上一页、页码、下一页、末页                 |
| 切换每页条数  | 下拉选择，切换后从第一页重新加载                 |

---

### 3.2 F02 车辆资产详情页

#### 3.2.1 页面定位

展示车辆的全生命周期数据，以Tab标签形式组织不同维度的信息。用户可查看、编辑车辆资产的完整档案。

#### 3.2.2 页面结构

页面自上而下分为：
1. **顶部操作栏**：包含"关闭"按钮和"编辑"按钮
2. **Tab标签区**：展示各维度信息标签
3. **内容区**：展示当前选中Tab的详细内容
4. **底部操作栏**（编辑态）：包含"确定"和"取消"按钮，固定在页面底部

#### 3.2.3 Tab标签定义

| 序号 | Tab名称  | 本期建设 | 说明                              |
| -- | ------ | ---- | ------------------------------- |
| 1  | 基础信息   | 是    | 默认显示的Tab，展示车辆全维度基础字段            |
| 2  | 调拨信息   | 是    | 展示该车辆的调拨记录列表                   |
| 3  | 维保信息   | 否    | 本期不建设，Tab标签置灰显示，标注"暂未开放"        |
| 4  | 合规信息   | 否    | 本期不建设，Tab标签置灰显示，标注"暂未开放"        |
| 5  | 风险信息   | 否    | 本期不建设，Tab标签置灰显示，标注"暂未开放"        |
| 6  | 费用信息   | 否    | 本期不建设，Tab标签置灰显示，标注"暂未开放"        |
| 7  | 变更日志   | 是    | 展示该车辆的操作变更记录                   |

**默认显示：** 基础信息Tab。

#### 3.2.4 顶部操作栏

| 按钮   | 类型   | 可见角色             | 行为                                |
| ---- | ---- | ---------------- | --------------------------------- |
| 关闭   | 次按钮  | 所有角色             | 关闭当前详情页面，返回列表页                    |
| 编辑   | 主按钮  | 管理员、项目经理         | 进入当前Tab页面的编辑状态，底部显示固定操作栏（确定/取消）  |

#### 3.2.5 编辑态规则

1. 点击"编辑"后，仅当前Tab页面进入编辑态，其他Tab页面不受影响
2. 编辑态下，底部显示固定操作栏，包含"确定"和"取消"按钮，按钮右侧对齐
3. 点击"确定"：执行当前Tab页面内容校验，校验成功则保存并记录变更日志，校验失败则显示错误提示
4. 点击"取消"：退出编辑态，不保存任何修改
5. 编辑保存前需要对页面内容进行校验，校验失败则保存失败
6. 保存成功后，自动记录变更日志（修改字段、修改前后值、操作人、操作时间等）

---

### 3.3 F03 基础信息Tab

#### 3.3.1 页面定位

展示车辆的完整基础档案信息，涵盖车辆从车型到硬件配置的全维度数据。查看态为只读展示，编辑态可修改字段。

#### 3.3.2 信息分组

基础信息Tab围绕车辆的车型、车辆等基础信息展开，分为以下16个信息分组，各分组使用折叠面板或分组标题分隔：

| 序号 | 分组名称   | 说明                       |
| -- | ------ | ------------------------ |
| 1  | 车辆基础信息 | 车架号、车牌号等车辆标识信息           |
| 2  | 归属信息   | 项目归属、资产来源等归属信息           |
| 3  | 车辆照片   | 车辆外观照片                   |
| 4  | 行驶证信息  | 行驶证照片及注册信息               |
| 5  | 车型信息   | 车型名称、版本及关联自动获取的车型参数      |
| 6  | 整车信息   | 整车铭牌、制造日期等               |
| 7  | 底盘信息   | 底盘铭牌、轮胎型号等               |
| 8  | 主发动机信息 | 主发动机铭牌照片                 |
| 9  | 副发动机信息 | 副发动机铭牌、厂家、型号、功率等         |
| 10 | 变速箱信息  | 变速箱铭牌、厂家、型号等             |
| 11 | 上装信息   | 风机、水泵等上装设备信息             |
| 12 | 其他     | 其他配置附件、备注                |
| 13 | 硬件信息   | 硬件设备列表，可动态增减行            |
| 14 | 软件信息   | 软件信息列表，可动态增减行            |
| 15 | 摄像头信息  | 摄像头参数列表，可动态增减行           |
| 16 | 车辆功能配置 | 功能类型、历史图片/视频、实时视频等配置信息   |

#### 3.3.3 字段定义

各分组字段定义与车辆建档模块中"新增车辆信息"的字段一致（详见车辆建档PRD 3.2.3节），此处不再重复罗列。

**与建档模块字段的关系说明：**
- 查看态：所有字段只读展示，附件类字段支持点击预览/下载
- 编辑态：字段可编辑，校验规则与建档模块一致
- 车型信息分组：车型名称、车型版本为下拉选择，确定后自动填充关联字段（只读）；其余车型信息字段不可手动编辑

#### 3.3.4 编辑态交互流程

1. 用户在详情页点击"编辑"→ 基础信息Tab进入编辑态
2. 可编辑字段变为可输入状态，必填项标红色星号
3. 车型名称选择后，车型版本下拉选项自动填充；选择车型版本后，其余车型信息字段自动填充（只读）
4. 硬件信息、软件信息、摄像头信息分组支持"添加行"和"删除行"操作
5. 点击"确定"：
   - 系统执行表单校验（必填项、格式校验等）
   - 校验成功 → 保存数据，记录变更日志（记录修改字段、修改前值、修改后值），退出编辑态，显示"保存成功"Toast提示
   - 校验失败 → 在对应字段下方显示红色错误提示，编辑态不退出
6. 点击"取消" → 退出编辑态，不保存任何修改，恢复至修改前的数据

---

### 3.4 F04 调拨信息Tab

#### 3.4.1 页面定位

以列表形式展示该车辆的调拨记录，记录车辆在不同项目间的流转历史。

#### 3.4.2 页面结构

页面自上而下分为：
1. **搜索区**：调拨信息的查询条件
2. **数据列表区**：以表格形式展示调拨记录
3. **分页区**：展示分页控件与数据统计

#### 3.4.3 搜索区

查询条件与查询按钮放置在同一行显示。

| 元素      | 类型       | 配置说明                  |
| ------- | -------- | --------------------- |
| 调拨申请时间  | 日期范围选择器  | 选择起止日期                |
| 确认交接时间  | 日期范围选择器  | 选择起止日期                |
| 申请人     | 文本输入框    | 支持模糊搜索                |
| 调拨前项目   | 下拉单选     | 选项来自项目列表              |
| 调拨后项目   | 下拉单选     | 选项来自项目列表              |
| 查询按钮    | 主按钮      | 点击执行查询，列表刷新为查询结果      |
| 重置按钮    | 次按钮      | 点击重置所有查询条件至默认状态（所有条件为空）|

#### 3.4.4 数据列表区

**列表显示字段（从左到右）：**

| 序号 | 字段名      | 字段标识               | 宽度建议   | 对齐方式 | 说明         |
| -- | -------- | ------------------ | ------ | ---- | ---------- |
| 0  | 复选框      | checkbox           | 50px   | 居中   | 支持全选/单选，用于导出操作 |
| 1  | 任务ID     | task_id            | 140px  | 左对齐  |            |
| 2  | 调拨申请时间   | apply_time         | 180px  | 左对齐  | 格式：YYYY-MM-DD HH:mm:ss |
| 3  | 确认交接时间   | confirm_time       | 180px  | 左对齐  | 格式：YYYY-MM-DD HH:mm:ss |
| 4  | 申请人      | applicant          | 120px  | 左对齐  |            |
| 5  | 调拨前项目    | from_project       | 160px  | 左对齐  |            |
| 6  | 调拨后项目    | to_project         | 160px  | 左对齐  |            |
| 7  | 操作       | actions            | 100px  | 居中   | 详情         |

**排序规则：** 默认按调拨申请时间由近到远倒序排列。

**操作列按钮：**

| 操作   | 触发行为               | 可见角色  |
| ---- | ------------------ | ----- |
| 详情   | 打开调度信息详情页面（本期暂不开发） | 所有角色  |

**行交互：** 行hover高亮。

#### 3.4.5 分页区

| 配置项     | 规则                                |
| ------- | --------------------------------- |
| 每页条数选项  | 10、20、50、100，默认10                |
| 显示信息    | 总记录数、当前页码/总页数                    |
| 翻页控件    | 首页、上一页、页码、下一页、末页                 |
| 切换每页条数  | 下拉选择，切换后从第一页重新加载                 |

#### 3.4.6 导出功能

| 触发条件         | 行为                                      |
| ------------ | --------------------------------------- |
| 用户选中了特定数据    | 导出用户选择的数据，不支持跨页导出                       |
| 用户未选择特定数据    | 弹出提示："是否导出所有数据？"，用户选择"是"则导出当前筛选条件下的所有数据，选择"否"则不执行操作 |

导出权限：管理员、项目经理、财务人员。

---

### 3.5 F05 变更日志Tab

#### 3.5.1 页面定位

展示该车辆的所有操作变更记录，包括修改和删除行为的完整日志，实现数据可溯源。

#### 3.5.2 日志记录规则

**修改行为记录：**

| 记录字段   | 说明               |
| ------ | ---------------- |
| 操作对象   | 被修改的车辆（车架号/VIN码） |
| 操作类型   | 修改               |
| 修改字段   | 被修改的字段名称         |
| 修改前    | 修改前的值            |
| 修改后    | 修改后的值            |
| 操作人    | 执行修改的用户          |
| 操作时间   | 修改操作的时间          |
| 操作来源   | 操作来源标识（如：Web端、APP端等）|

**删除行为记录：**

| 记录字段   | 说明               |
| ------ | ---------------- |
| 操作对象   | 被删除的车辆（车架号/VIN码） |
| 操作类型   | 删除               |
| 操作人    | 执行删除的用户          |
| 操作时间   | 删除操作的时间          |
| 操作来源   | 操作来源标识           |

#### 3.5.3 列表显示

以时间线或列表形式展示变更日志记录，默认按操作时间由近到远倒序排列。

| 序号 | 字段名   | 宽度建议   | 对齐方式 | 说明         |
| -- | ----- | ------ | ---- | ---------- |
| 0  | 复选框   | 50px   | 居中   | 支持全选/单选，用于导出操作 |
| 1  | 操作类型  | 100px  | 居中   | 修改/删除，使用不同颜色标签区分 |
| 2  | 修改字段  | 160px  | 左对齐  | 删除行为时显示"—"  |
| 3  | 修改前   | 200px  | 左对齐  | 删除行为时显示"—"  |
| 4  | 修改后   | 200px  | 左对齐  | 删除行为时显示"—"  |
| 5  | 操作人   | 120px  | 左对齐  |             |
| 6  | 操作时间  | 180px  | 左对齐  | 格式：YYYY-MM-DD HH:mm:ss |
| 7  | 操作来源  | 120px  | 左对齐  |             |

#### 3.5.4 导出功能

| 触发条件         | 行为                                      |
| ------------ | --------------------------------------- |
| 用户选中了特定数据    | 导出用户选择的数据，不支持跨页导出                       |
| 用户未选择特定数据    | 弹出提示："是否导出所有数据？"，用户选择"是"则导出当前筛选条件下的所有数据，选择"否"则不执行操作 |

导出权限：管理员、项目经理、财务人员。

导出格式：.xlsx，文件命名规则：变更日志_YYYYMMDD.xlsx

---

### 3.6 F06 车辆分配弹窗

#### 3.6.1 触发方式

用户在列表页勾选车辆后，点击"车辆分配"按钮打开弹窗。

#### 3.6.2 弹窗规格

| 属性   | 值           |
| ---- | ----------- |
| 宽度   | 700px       |
| 最大宽度 | 90vw        |
| 位置   | 居中弹出        |
| 遮罩   | 半透明，点击遮罩不关闭 |
| 标题   | 车辆分配        |

#### 3.6.3 前置校验

| 校验条件                          | 提示信息              |
| ----------------------------- | ----------------- |
| 用户未勾选任何车辆                     | "当前未选择任何车辆"       |
| 用户选中的车辆中存在车辆状态不为"待分配"的车辆     | "已分配的车辆请走车辆调拨流程"  |

#### 3.6.4 弹窗内容

弹窗中以列表形式展示用户已选择的车辆信息：

| 序号 | 字段名     | 类型     | 说明                      |
| -- | ------- | ------ | ----------------------- |
| 1  | 车架号     | 文本显示   |                         |
| 2  | 网联编号    | 文本显示   |                         |
| 3  | 别名      | 文本显示   |                         |
| 4  | 当前所属项目  | 文本显示   |                         |
| 5  | 分配至项目   | 下拉单选   | 选项来自项目列表；支持一键全选（统一分配至同一项目），也支持单个选择 |

#### 3.6.5 交互流程

1. 用户在列表页勾选车辆 → 点击"车辆分配"按钮
2. 系统校验勾选车辆的车辆状态
3. 校验通过 → 打开车辆分配弹窗
4. 弹窗展示已选车辆列表，每行显示车架号、网联编号、别名、当前所属项目、分配至项目
5. 用户选择"分配至项目"：
   - 支持一键全选：将所有车辆统一分配至同一项目
   - 支持单个选择：逐行选择不同的目标项目
6. 点击"确定"：
   - 系统执行分配操作，变更车辆的所属项目字段
   - 分配成功 → 关闭弹窗，显示"分配成功"Toast提示，列表刷新
7. 点击"取消" → 不执行操作，关闭弹窗

---

### 3.7 F07 删除确认

#### 3.7.1 触发方式

用户点击列表页操作列"删除"按钮。

#### 3.7.2 交互流程

1. 用户点击"删除" → 弹出二次确认弹窗
2. 确认弹窗内容："确定要删除该车辆档案信息吗？删除后不可恢复。"
3. 点击"确定" → 执行逻辑删除，记录删除操作日志，列表刷新
4. 点击"取消" → 关闭确认弹窗，不执行删除

#### 3.7.3 删除权限规则

| 角色   | 可删除范围         |
| ---- | ------------- |
| 管理员  | 所有车辆档案信息      |
| 项目经理 | 权限范围内的车辆      |
| 其他角色 | 无删除权限，操作按钮不可见 |

#### 3.7.4 删除方式

仅做逻辑删除（标记删除状态），不物理删除数据。删除后记录删除操作日志。

---

### 3.8 F08 导出功能

#### 3.8.1 适用范围

资产台账列表页、调拨信息Tab页、变更日志Tab页均支持导出功能。

#### 3.8.2 导出规则

| 触发条件         | 行为                                      |
| ------------ | --------------------------------------- |
| 用户选中了特定数据    | 导出用户选择的数据，不支持跨页导出                       |
| 用户未选择特定数据    | 弹出提示："是否导出所有数据？"，用户选择"是"则导出当前筛选条件下的所有数据，选择"否"则不执行操作 |

#### 3.8.3 导出权限

| 角色   | 导出权限 |
| ---- | ---- |
| 管理员  | 可导出  |
| 项目经理 | 可导出  |
| 财务人员 | 可导出  |
| 其他角色 | 不可导出 |

#### 3.8.4 导出格式

- 文件格式：.xlsx
- 文件命名规则：车辆资产台账_YYYYMMDD.xlsx（列表页）/ 调拨记录_YYYYMMDD.xlsx（调拨信息Tab）/ 变更日志_YYYYMMDD.xlsx（变更日志Tab）
- 导出字段与列表显示字段一致

---

## 4. 业务规则汇总

### 4.1 编辑规则

1. 车辆资产详情页在进行编辑时，编辑仅对当前Tab页生效
2. 点击了某个Tab页后进行编辑，仅编辑当前Tab页内容
3. 提交后仅保存当前Tab页内容
4. 保存前需对页面内容进行校验，校验失败则保存失败
5. 保存成功后自动记录变更日志

### 4.2 操作日志记录规则

1. 系统需要对用户对车辆的操作行为进行记录，包括修改和删除
2. 修改行为记录：操作对象、操作类型、修改字段、修改前、修改后、操作人、操作时间、操作来源，并在"变更日志"Tab中体现
3. 删除行为记录：操作对象、操作类型、操作人、操作时间、操作来源
4. 变更日志按操作时间倒序排列

### 4.3 车辆分配规则

1. 仅车辆状态为"待分配"的车辆支持分配操作
2. 已分配的车辆需走车辆调拨流程（非本模块处理）
3. 分配操作变更车辆的"归属项目"字段
4. 分配成功后车辆状态由"待分配"变更为"在库-正常使用"

### 4.4 车辆状态枚举

| 枚举值            | 说明          |
| -------------- | ----------- |
| 在库-闲置留用     | 闲置但保留使用     |
| 在库-闲置可调出         | 闲置可调配至其他项目  |
| 在库-维修  | 车辆维修中       |
| 在库-保养中          | 车辆保养中     |
| 在库-正常使用             | 车辆在库，车辆正常使用中      |
| 在库-待报废             | 车辆待提出报废申请      |
| 待分配            | 归属项目为空，待分配  |
| 已报废            | 报废申请审批通过  |
| 已处置            | 处置流程审批通过  |

### 4.5 资产来源枚举

| 枚举值   | 说明   |
| ----- | ---- |
| 外购    | 外部采购 |
| 租赁    | 租赁使用 |
| 融资租赁  | 融资租赁 |
| 借用    | 借用   |
| 自研    | 自主研发 |

### 4.6 导出规则

1. 选中导出：仅导出当前页选中的数据，不支持跨页导出
2. 全量导出：导出当前筛选条件下的所有数据
3. 未选择数据时弹出确认提示

---

## 5. 页面交互流程图

### 5.1 资产台账列表页流程

```
用户进入车辆资产管理
  → 显示资产台账列表页（默认加载第一页数据）
  → 可选操作：
    ├── 输入查询条件 → 点击查询 → 列表刷新
    ├── 点击重置 → 恢复默认查询条件
    ├── 点击编辑 → 打开详情页编辑态（基础信息Tab）
    ├── 勾选车辆 → 点击车辆分配 → 校验 → 打开分配弹窗
    ├── 点击详情 → 打开资产详情页（查看态）
    ├── 点击删除 → 二次确认 → 逻辑删除
    └── 勾选/不勾选 → 点击导出 → 导出数据
```

### 5.2 资产详情页流程

```
用户进入资产详情页（默认显示基础信息Tab）
  → 可选操作：
    ├── 切换Tab标签 → 显示对应Tab内容
    │   ├── 基础信息 → 查看/编辑车辆全维度信息
    │   ├── 调拨信息 → 查看调拨记录列表 → 查询/导出
    │   ├── 变更日志 → 查看操作变更记录
    │   └── 维保/合规/风险/费用 → 置灰，暂未开放
    ├── 点击编辑 → 当前Tab进入编辑态 → 修改 → 确定/取消
    └── 点击关闭 → 返回列表页
```

### 5.3 车辆分配流程

```
用户勾选车辆 → 点击车辆分配
  → 校验：
  ├── 未勾选 → 提示"当前未选择任何车辆"
  ├── 存在非"待分配"车辆 → 提示"已分配的车辆请走车辆调拨流程"
  └── 校验通过 → 打开分配弹窗
    → 选择分配至项目（全选/单选）
    → 点击确定 → 执行分配 → 关闭弹窗 → 列表刷新
    → 点击取消 → 关闭弹窗
```

---

## 6. 非功能需求

### 6.1 性能要求

| 指标     | 要求                |
| ------ | ----------------- |
| 列表加载   | 首屏加载时间≤2s         |
| 详情页加载  | 页面加载时间≤2s         |
| 编辑保存   | 提交响应时间≤3s         |
| 查询响应   | 查询结果返回时间≤2s       |
| 导出响应   | 导出文件生成时间≤10s      |

### 6.2 安全要求

| 指标     | 要求                              |
| ------ | ------------------------------- |
| 权限控制   | 按角色控制功能权限，按项目控制数据权限             |
| 操作审计   | 记录所有编辑、删除操作的操作人、操作时间、操作内容、操作来源  |
| 数据安全   | 逻辑删除，不物理删除；变更日志不可篡改             |

### 6.3 兼容性要求

| 指标     | 要求                            |
| ------ | ----------------------------- |
| 浏览器    | Chrome 80+、Edge 80+、Firefox 80+ |
| 分辨率    | 1366×768、1920×1080            |
| 导出格式   | 仅支持.xlsx                      |

---

## 7. 关联模块说明

| 关联模块   | 关联关系                  | 说明                        |
| ------ | --------------------- | ------------------------- |
| 车辆建档   | 数据来源                  | 建档数据流转至资产管理模块，形成资产台账      |
| 车型管理   | 车型信息字段引用车型表数据         | 车型名称选择后自动获取关联字段           |
| 项目管理   | 归属项目字段引用项目列表数据        | 项目数据来源待确认                 |
| 车辆调拨   | 调拨信息Tab数据来源           | 调拨记录由调拨模块产生，本模块仅展示        |
| 费务管理   | 财务人员维护资产分类等字段         | 财务字段由财务人员维护               |

---

## 8. 已确认项与待确认项

### 8.1 已确认项

| 编号 | 确认内容                              | 确认结果                          | 确认日期     |
| -- | ---------------------------------- | ----------------------------- | -------- |
| 1  | 详情页中维保/合规/风险/费用信息Tab本期是否建设        | 本期不建设，仅展示Tab标签入口（置灰）          | 2026-05-29 |
| 2  | 调拨信息中"详情"按钮的功能                    | 本期暂不开发，仅预留按钮入口                | 2026-05-29 |
| 3  | 基础信息Tab字段定义与建档模块的关系               | 字段定义一致，直接引用建档模块的字段设计          | 2026-05-29 |
| 4  | 车辆分配前置校验（需求文档列表页与详情页描述不一致）       | 采用详情页的完整描述，包含"非待分配"车辆校验        | 2026-05-29 |
| 5  | 车辆分配后，车辆状态变更规则                   | 分配后状态由"待分配"变更为"在库-正常使用"        | 2026-05-29 |
| 6  | 变更日志是否支持导出                       | 支持导出，导出规则与调拨信息Tab一致            | 2026-05-29 |
| 7  | 列表页"编辑"按钮操作方式                    | 不需要先勾选车辆，直接进入编辑态               | 2026-05-29 |
| 8  | 调拨信息Tab中调拨记录的详情页                | 后续版本开发，本期仅预留按钮入口               | 2026-05-29 |

### 8.2 待确认项

当前无待确认项，所有疑问均已确认。

---

## 9. 验收标准

### 9.1 资产台账列表页验收

- [ ] 列表页正确显示14个字段+复选框+操作列
- [ ] 默认按注册时间倒序排列
- [ ] 查询功能：默认显示前4个查询条件，展开后显示全部14个查询条件
- [ ] 重置功能：点击后恢复所有查询条件为空
- [ ] 分页功能：支持10/20/50/100切换，显示总记录数和页码
- [ ] 复选框功能：支持全选/单选
- [ ] 车辆状态标签使用不同颜色区分
- [ ] 操作按钮按角色权限正确显示/隐藏
- [ ] 数据权限按角色正确过滤

### 9.2 资产详情页验收

- [ ] 详情页正确显示7个Tab标签（含4个置灰Tab）
- [ ] 默认显示基础信息Tab
- [ ] 基础信息Tab正确展示16个信息分组
- [ ] 点击"编辑"进入当前Tab编辑态，底部操作栏显示
- [ ] 编辑态仅对当前Tab生效
- [ ] 保存前校验，校验失败显示错误提示
- [ ] 保存成功记录变更日志
- [ ] 点击"关闭"返回列表页

### 9.3 调拨信息Tab验收

- [ ] 调拨记录列表正确显示7个字段+操作列
- [ ] 默认按调拨发起时间倒序排列
- [ ] 查询功能：5个查询条件可单独或组合查询
- [ ] 重置功能：点击后恢复所有查询条件为空
- [ ] 分页功能正常
- [ ] 导出功能：选中导出和全量导出正常
- [ ] "详情"按钮显示但本期不开放跳转

### 9.4 变更日志Tab验收

- [ ] 变更日志正确展示修改和删除记录
- [ ] 修改记录包含：修改字段、修改前、修改后、操作人、操作时间、操作来源
- [ ] 删除记录包含：操作人、操作时间、操作来源
- [ ] 默认按操作时间倒序排列

### 9.5 车辆分配验收

- [ ] 未勾选车辆时点击分配，提示"当前未选择任何车辆"
- [ ] 勾选非"待分配"车辆时，提示"已分配的车辆请走车辆调拨流程"
- [ ] 弹窗正确展示已选车辆列表
- [ ] 支持一键全选分配至同一项目
- [ ] 支持单个选择不同项目
- [ ] 确定后变更车辆归属项目
- [ ] 取消后关闭弹窗，不执行操作

### 9.6 删除验收

- [ ] 删除前弹出二次确认弹窗
- [ ] 管理员可删除所有车辆
- [ ] 项目经理可删除权限范围内的车辆
- [ ] 其他角色无删除按钮
- [ ] 删除为逻辑删除
- [ ] 删除后记录操作日志

### 9.7 导出验收

- [ ] 选中数据后导出，仅导出选中数据
- [ ] 未选中数据时弹出确认提示
- [ ] 确认全量导出时，导出当前筛选条件下所有数据
- [ ] 不支持跨页导出
- [ ] 导出格式为.xlsx
- [ ] 导出权限按角色正确控制
