# 车辆实时监管 PRD

| 属性 | 内容 |
| --- | --- |
| 产品名称 | 车辆管理系统 |
| 文档范围 | 车辆监管一张图、车辆运行数据、车辆故障信息、车辆异常记录 |
| 文档版本 | V1.0 |
| 创建日期 | 2026-09-15 |
| 文档状态 | 待确认 |

## 1. 背景与目标

车辆实时监管用于查看车辆网联终端上报的运行、故障和异常数据，支持运营人员按车辆、项目和时间快速定位数据，并将查询结果或选中记录导出用于复盘。

本文档对应 `01-需求文档/车辆实时监管.md` 第 2.2、2.3、2.4 节。原“车辆异常告警”统一命名为“车辆异常记录”。

## 2. 功能范围

### 2.0 车辆监管一张图

#### 2.0.1 地图与点位

| 类别 | 要求 |
| --- | --- |
| 底图 | 使用 Leaflet 1.9.4，本地加载 JS/CSS；地图瓦片使用 OpenStreetMap，保留版权标识 |
| 坐标 | 终端位置按 WGS84 坐标展示；高保真阶段忽略无效、缺失或过期坐标 |
| 聚合 | 默认按市聚合；市级聚合点显示车辆总数。点击聚合点后地图缩放至车辆级别并展示该市车辆点位；缩放层级低于车辆级别时恢复市级聚合 |
| 车辆图标 | 按车型使用图标；车辆标识按车牌号、网联编号、VIN 码优先级显示。平行驾驶为绿色，自动驾驶为蓝色，人工驾驶为灰色 |
| 异常角标 | 显示当日故障数与异常事件数之和；数量大于 0 时显示角标并叠加闪烁效果 |
| 底部图例 | 展示平行驾驶、自动驾驶、人工驾驶三种颜色图例 |

#### 2.0.2 搜索、筛选与联动

- 地图右上角默认仅展示搜索、筛选两个图标按钮；点击后分别展开搜索框和筛选浮层，同一时间仅展开一个面板，点击地图空白处或按 `Esc` 关闭。
- 搜索框支持 VIN 码、车牌号、网联编号精确搜索，搜索结果聚焦并选中车辆。
- 筛选浮层中的项目、区域、车型均使用可多选下拉框；不同筛选项之间按 AND 组合，同一筛选项内部按 OR 组合；选项来自全量主数据。
- “仅显示故障与异常车辆”按照当日故障数或异常事件数大于 0 判断。
- 数据面板固定在地图左侧，支持收起；车辆列表和异常与故障两个 Tab 均不设置独立查询条件组，仅保留按项目、车型、区域切换的分组维度，默认按项目分组并打开车辆列表 Tab。
- 地图右上角的搜索和筛选结果同步作用于地图、车辆列表、异常与故障列表；切换分组维度不改变当前搜索及筛选条件。列表选车或选择异常/故障卡片后，地图聚焦并选中对应车辆；地图选车后车辆列表同步高亮。

#### 2.0.3 车辆信息弹窗

- 标题按“车牌号/网联编号/VIN 码 - 车型 - 所属项目”展示。
- “实时数据”Tab 展示驾驶模式、接管状态、车辆速度、横向加速度、纵向加速度、侧倾角速度、横摆角速度、档位和数据采集时间。
- “故障与异常”Tab 以记录卡片展示，按上报时间倒序排列，支持查看更多。
- 轨迹回放不打开全屏或独立地图弹窗，直接在车辆监管一张图的原地图窗口内绘制；页面顶部导航、左侧导航和车辆数据面板保持可见。进入回放时隐藏原地图的统计、搜索筛选、普通图例及车辆聚合点，显示轨迹图例、地图工具和底部回放控制台；关闭后恢复进入回放前的地图视角和普通车辆点位。
- 回放控制台默认查询当天轨迹，支持选择日期、开始时间、结束时间和车辆；结束时间必须晚于开始时间。查询后在地图绘制起点、终点、完整轨迹、已播放轨迹和当前车辆位置。
- 回放支持播放/暂停、0.5×/1×/2×/4×倍速和拖动时间轴；播放或拖动时同步更新当前时间、车辆速度、驾驶模式和地图车辆位置，并提供视频回看入口。
- 视频在当前地图页面内打开播放弹窗，按车辆档案中的摄像头信息展示多摄像头画面。

#### 2.0.4 高保真阶段边界

- 高保真阶段不实现实时推送、刷新频率、数据过期判定、接口失败重试和权限审计，使用本地模拟数据完成交互验证。
- 轨迹播放和视频播放使用可交互模拟内容，后续接入真实接口和播放协议。

### 2.1 车辆运行数据

#### 2.1.1 页面结构

页面采用左右分栏。左侧展示可查询、分组和选中的车辆列表，右侧展示当前车辆的运行数据明细。

#### 2.1.2 车辆列表

| 类别 | 要求 |
| --- | --- |
| 查询条件 | VIN 码、车牌号、车型名称、项目；默认仅显示 VIN 码，车牌号、车型名称和项目通过“展开更多/收起”切换显示 |
| 分组方式 | 支持按项目或车型名称切换，默认按项目分组 |
| 无归属数据 | 当前分组字段为空时统一进入“无分组” |
| 分组交互 | 显示组内车辆数量，支持展开和收起；初始化、查询重置和切换分组方式时，当前选中车辆所在分组置于首位并仅展开该首个分组，其余分组收起 |
| 默认选择 | 选中当前查询结果中 VIN 码排序最前的车辆 |
| 联动 | 选中其他车辆后，右侧切换为该车辆的运行明细 |

#### 2.1.3 运行数据明细

查询条件为数据采集时间范围。开始时间晚于结束时间时不执行查询，并给出错误提示。

| 序号 | 列表字段 |
| ---: | --- |
| 1 | VIN 码 |
| 2 | 所属项目 |
| 3 | 经度 |
| 4 | 纬度 |
| 5 | 驾驶模式 |
| 6 | 接管状态 |
| 7 | 车辆速度 |
| 8 | 车辆横向加速度 |
| 9 | 车辆纵向加速度 |
| 10 | 车辆侧倾角速度 |
| 11 | 车辆横摆角速度 |
| 12 | 档位 |
| 13 | 物联网终端在线状态 |
| 14 | SOC(%) |
| 15 | 数据采集时间 |
| 16 | 车辆作业模式 |

车辆作业模式的可用值为：充电、休眠、转场、停靠、关机、工作。

### 2.2 车辆故障信息

车辆故障信息用于展示车辆主动上报的故障码数据，主要反映车辆软硬件问题。

| 类别 | 内容 |
| --- | --- |
| 查询条件 | VIN 码、车牌号、网联编号、所属项目、故障码、故障上报时间范围 |
| 列表字段 | VIN 码、车牌号、网联编号、所属项目、故障码、故障定义、故障上报时间 |

查询区不使用独立外框，表格上方不展示额外说明文字，仅保留选择反馈和导出操作。

### 2.3 车辆异常记录

车辆异常记录用于展示车辆触发预先设定的策略规则后生成的异常事件。

| 类别 | 内容 |
| --- | --- |
| 查询条件 | VIN 码、车牌号、网联编号、所属项目、异常类型、异常上报时间范围 |
| 异常类型 | 超时停靠、超速行驶、限行区行驶 |
| 列表字段 | VIN 码、车牌号、网联编号、所属项目、异常类型、开始时间、结束时间、持续时长（min）、异常上报时间 |

## 3. 公共交互规则

1. 查询后列表总数、当前页和分页状态同步更新，并清空原有勾选。
2. 重置清空当前页面的查询条件、勾选和页码，恢复全部数据。
3. 列表默认每页 10 条，支持切换每页条数；显示总记录数和当前页码。
4. 全选仅对当前页生效，单条勾选在切换页码后保留，页面显示已选数量。
5. 当存在勾选数据时，点击“导出”仅导出选中记录。
6. 未勾选数据时，点击“导出”须二次确认，确认后导出当前查询条件下的全部记录。
7. 查询无结果时显示空状态；无可导出数据时不生成空文件并给出提示。
8. 导出字段、字段顺序与当前页面列表字段保持一致，不包含复选框列。

## 4. 页面与导航

| 导航路径 | 页面文件 |
| --- | --- |
| 运行监管 > 实时监管 > 车辆运行数据 | `03-高保真页面/monitor/vehicle-running-data.html` |
| 运行监管 > 异常事件 > 车辆故障信息 | `03-高保真页面/monitor/vehicle-fault-info.html` |
| 运行监管 > 异常事件 > 车辆异常记录 | `03-高保真页面/monitor/vehicle-anomaly-records.html` |

## 5. 验收标准

1. 四个页面可从页面索引和公共侧边导航进入，导航高亮与当前页一致。
2. 查询、重置、分页、每页条数、单选和当前页全选均可交互。
3. 车辆运行数据页的左侧查询区默认仅显示 VIN 码，可展开或收起其余条件；页面默认按项目分组并选中 VIN 最小的车辆，仅展开首个分组；切换车辆时右侧数据联动更新。
4. 车辆异常记录页不再显示“车辆异常告警”旧名称。
5. 已勾选导出与未勾选导出的范围、确认流程和字段均符合第 3 节规则。
6. 1366×768 和 1920×1080 视口下筛选区、操作区和分页不重叠，长表格在内容区内横向滚动，不引起页面级溢出。
7. 车辆监管一张图右上角默认仅显示搜索和筛选图标，展开面板互斥；项目、区域、车型以多选下拉框呈现。左侧两个 Tab 均仅保留分组维度，并与地图搜索、筛选结果同步。
8. 轨迹回放直接在原地图窗口内绘制且不出现页面级遮罩或独立地图；日期、起止时间、车辆查询、播放暂停、倍速、时间轴拖动、状态联动、图例显隐、轨迹定位、关闭和视频回看均可交互，关闭后恢复原地图视角与车辆点位。

## 6. 版本记录

| 日期 | 版本 | 说明 |
| --- | --- | --- |
| 2026-09-15 | V1.0 | 新增车辆监管一张图、车辆运行数据、车辆故障信息和车辆异常记录页面需求；一张图明确使用 Leaflet + OpenStreetMap、WGS84、按市聚合、地图内轨迹/视频弹窗及筛选联动；一张图左侧车辆列表、异常与故障两个 Tab 移除独立查询条件组，仅保留分组维度，统一联动地图右上角搜索与多选下拉筛选，工具栏默认收起为搜索/筛选图标；运行数据角速度字段明确为“车辆侧倾角速度”和“车辆横摆角速度”；原“车辆异常告警”更名为“车辆异常记录”；车辆故障信息页移除表格上方的说明文字和查询区独立外框；车辆运行数据页左侧查询条件支持展开收起且默认仅显示 VIN 码，组织树默认仅展开首个分组；车辆详情弹窗调整为 640px 紧凑宽度，标题采用 16px 单行省略并优化内容间距。 |
| 2026-09-16 | V1.0 | 轨迹回放调整为直接在车辆监管一张图的原地图窗口内绘制，不使用全屏或独立地图弹窗；保留页面导航与车辆数据面板，补充日期、起止时间、车辆查询、图例、完整轨迹定位、视频回看、播放暂停、倍速、时间轴拖动及时间/速度/驾驶模式/车辆位置联动，关闭后恢复原地图视角与车辆点位。 |
